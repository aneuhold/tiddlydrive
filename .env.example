# Example environment for local development with Netlify Dev
# Copy to .env and populate values.
# Netlify CLI will load this when you run `pnpm dev:local`.

# Google OAuth Client ID (Web) from Google Cloud Console
GOOGLE_CLIENT_ID=477983451498-28pnsm6sgqfm5l2gk0pris227couk477.apps.googleusercontent.com

# Google OAuth Client Secret from Google Cloud Console
GOOGLE_CLIENT_SECRET=YOUR_CLIENT_SECRET

# Public callback URL for your Netlify Dev server
# In prod, this should be: https://tiddlydrive.tonyneuhold.com/.netlify/functions/oauth-callback
OAUTH_REDIRECT_URI=http://localhost:4317/.netlify/functions/oauth-callback

# Base64url-encoded 32-byte key used to encrypt the refresh token cookie
# Generate with: openssl rand -base64 32 | tr '+/' '-_' | tr -d '='
TD2_ENC_KEY_B64=YOUR_32_BYTE_BASE64URL_KEY
